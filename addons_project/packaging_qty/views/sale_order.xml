<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="view_order_form_add_packaging_qty" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="priority">100</field>
	    <field name="inherit_id" ref="sale_stock.view_order_form_inherit_sale_stock"/>
        <field name="arch" type="xml">
		<field name="product_packaging_id" position="after">
			<field name="product_packaging_qty"/>
		</field>
        </field>
    </record>

        <!-- <record id="view_form_sale_order_line_add_pack_info" model="ir.ui.view">
            <field name="name">sale.order form add pack_info</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="model">sale.order</field>
            <field name="type">form</field>
            <field name="arch" type="xml"> -->
			    <!--so.line tree 视图中添加pack信息-->
                <!-- <xpath expr="//field[@name='order_line']/tree/field[@name='product_uom_qty']" position="before">
                    <field name="product_packaging_id" attrs="{'invisible': [('product_id', '=', False)]}" context="{'default_product_id': product_id, 'tree_view_ref':'product.product_packaging_tree_view', 'form_view_ref':'product.product_packaging_form_view'}" domain="[('product_id','=',product_id)]" groups="product.group_stock_packaging" options="{'no_create': True}"/>
                    <field name="product_packaging_qty" attrs="{'invisible':[('product_packaging_id','=',False)]}"/>
			    </xpath>
            </field>
        </record> -->

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_form_sale_order_line_add_pack_info" model="ir.ui.view">
        <field name="name">sale.order form add pack_info</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/list/field[@name='product_uom_qty']" position="before">
                <field name="product_uom_ids" column_invisible="True"/>
                <field name="product_packaging_id" invisible="not product_id" domain="[('id','in', product_uom_ids)]" options="{'no_create': True}"/>
                <field name="product_packaging_qty" invisible="not product_packaging_id"/>
            </xpath>
        </field>
    </record>
</odoo>
